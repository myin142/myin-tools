image: node

cache:
    paths:
        - node_modules/

stages:
    - build
    - publish

build:
    stage: build
    script:
        - npm i
        - npm run build
    artifacts:
        paths:
            - dist/packages
    only:
        - tags

publish:
    stage: publish
    dependencies:
        - build
    script:
        - npm publish dist/packages/aws-cdk
    only:
        - tags
